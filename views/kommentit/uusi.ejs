<% include ../partials/header %>

<div class="ui main text container segment">
    <div class="ui huge header">Lisää uusi kommentti </div>
        <form class="ui form" action="/paikat/<%= place._id %>/kommentit" method="POST">
            <h3 class="ui dividing header"><%= place.title %></h3>
            <div class="field">
               <input type="text" name="comment[text]" placeholder="kirjoita kommentti">
            </div>
            <button class="ui teal basic button" type="submit">Lähetä kommentti</button>
        </form>
</div>


<% include ../partials/footer %>